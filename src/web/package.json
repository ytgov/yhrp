{
  "name": "vue3-template",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm run build:translations && vite",
    "build": "npm run build:translations && vite build",
    "build:docker": "npm run build:translations && vite build --outDir ../api/dist/web/ --emptyOutDir",
    "build:translations": "node scripts/build-translations.js",
    "preview": "vite preview",
    "test": "vitest",
    "test:ui": "vitest --ui",
    "test:run": "vitest run",
    "generate:mock-data": "node src/modules/places/data/mock/convert-markdown.js"
  },
  "dependencies": {
    "@mdi/font": "^7.1.96",
    "leaflet": "^1.9.4",
    "vue": "^3.5.16",
    "vue-router": "^4.5.1",
    "vuetify": "^4.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.4",
    "@vitest/ui": "^3.1.4",
    "eslint": "^9.28.0",
    "eslint-plugin-vue": "^10.1.0",
    "jsdom": "^26.1.0",
    "sass-embedded": "^1.89.1",
    "vite": "^6.3.5",
    "vite-plugin-vuetify": "^2.1.3",
    "vitest": "^3.1.4"
  }
}
